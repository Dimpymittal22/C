#include<stdio.h>
int main()
{
    int area,b,h;
    printf("Enter the values of breadth and height:");
    scanf("%d%d", &b,&h);
    area=0.5*b*h;
    printf("The area of triangle is:%f",0.5*b*h);
}
    